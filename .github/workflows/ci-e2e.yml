name: ci-e2e

# TODO(Phase 3+): Playwright E2E suite is meaningless before the bilingual
# transcript UI (Phase 3) exists. This workflow is a placeholder so the file
# structure from Blueprint Section 5/13 is in place; it currently does nothing
# beyond a checkout, and is intentionally not wired into required status checks.

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *" # nightly, per Blueprint Section 13.1 stage 6

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "No E2E specs yet — Phase 3 introduces apps/web/e2e Playwright specs."
