fastapi>=0.115.4,<0.142.0
uvicorn[standard]>=0.32.0,<0.36.0
pydantic>=2.9.2,<3.0.0
# Phase 7: orchestrator calls clinical-nlp's /summarize endpoint (real
# service-to-service HTTP call, not model inference itself -- see
# app/summary/client.py). Same lightweight base dependency speech-pipeline
# already uses for its own cross-service clients.
httpx>=0.27.2,<1.0.0
