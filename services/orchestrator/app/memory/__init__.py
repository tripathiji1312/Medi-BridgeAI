"""Rolling conversation memory + structured case memory (Blueprint Section
2.2), persisted per session only (in-process store; real Postgres/Redis
persistence is Phase 9 infra hardening, not this phase's scope)."""

